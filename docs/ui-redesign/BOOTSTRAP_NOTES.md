# Bootstrap notes

- Bootstrap tracker: #296.
- Production baseline at bootstrap: MyFinHub v1.3.0.
- Existing historical audit/evidence documents were deliberately retained; cleanup was conservative and non-destructive.
- `visual-qa/` remains the single current-state screenshot archive. The full-page sweep was extended to include the existing Needs Attention page so the primary desktop page inventory has a deterministic baseline.
- The two OLD/NEW direction images remain exact owner-supplied conversation attachments; their dimensions and SHA-256 identities are recorded in `references/direction/README.md`. They should be attached once to the long-lived Design Director chat rather than replaced by degraded reconstructions.
- Issue #266 was not mutated and is explicitly protected from both redesign chats.
