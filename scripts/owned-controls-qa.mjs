import { spawn, execFileSync } from 'node:child_process';

const baseUrl=process.env.RHEOMIQ_QA_URL||'http://127.0.0.1:5173/qa.html';
const chrome=execFileSync('bash',['-lc','command -v google-chrome || command -v chromium || command -v chromium-browser'],{encoding:'utf8'}).trim();
if(!chrome)throw new Error('Chrome/Chromium is required for owned-controls QA.');
const port=9224;const child=spawn(chrome,['--headless=new',`--remote-debugging-port=${port}`,'--remote-debugging-address=127.0.0.1','--user-data-dir=/tmp/rheomiq-owned-controls-qa','--no-sandbox','--disable-gpu','about:blank'],{stdio:'ignore'});const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
async function waitHttp(url){for(let i=0;i<60;i++){try{const response=await fetch(url);if(response.ok)return}catch{}await sleep(250)}throw new Error(`Timed out waiting for ${url}`)}
class Cdp{constructor(url){this.url=url;this.id=0;this.pending=new Map()}async open(){await new Promise((resolve,reject)=>{this.ws=new WebSocket(this.url);this.ws.onopen=resolve;this.ws.onerror=reject;this.ws.onmessage=event=>{const message=JSON.parse(event.data);if(!message.id)return;const pending=this.pending.get(message.id);if(!pending)return;this.pending.delete(message.id);message.error?pending.reject(new Error(message.error.message)):pending.resolve(message.result)}})}send(method,params={}){const id=++this.id;return new Promise((resolve,reject)=>{this.pending.set(id,{resolve,reject});this.ws.send(JSON.stringify({id,method,params}))})}async eval(expression){const result=await this.send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.text||'Runtime evaluation failed');return result.result.value}async call(functionDeclaration,args=[]){const root=await this.send('Runtime.evaluate',{expression:'globalThis'});const result=await this.send('Runtime.callFunctionOn',{objectId:root.result.objectId,functionDeclaration,arguments:args.map(value=>({value})),returnByValue:true,awaitPromise:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.text||'Runtime function call failed');return result.result.value}close(){this.ws?.close()}}
const assert=(value,message)=>{if(!value)throw new Error(`Owned controls QA assertion failed: ${message}`)};
try{await waitHttp(`http://127.0.0.1:${port}/json/version`);const target=await fetch(`http://127.0.0.1:${port}/json/new?${encodeURIComponent(baseUrl)}`,{method:'PUT'}).then(response=>response.json());const c=new Cdp(target.webSocketDebuggerUrl);await c.open();await c.send('Page.enable');await c.send('Runtime.enable');const viewport=(width,height)=>c.send('Emulation.setDeviceMetricsOverride',{width,height,deviceScaleFactor:1,mobile:width<=680});const waitFor=async(fn,label,args=[])=>{for(let i=0;i<70;i++){if(await c.call(fn,args))return;await sleep(100)}throw new Error(`Timed out waiting for ${label}`)};const clickText=async(selector,text)=>assert(await c.call("function(selector,text){const node=[...document.querySelectorAll(selector)].find(item=>(item.textContent||'').includes(text));if(!node)return false;node.click();return true}",[selector,text]),`missing ${text}`);await viewport(375,812);await c.send('Page.navigate',{url:baseUrl});await waitFor("function(){return Boolean(document.querySelector('#main-workspace h1'))}",'workspace');await clickText('button','Γρήγορη προσθήκη');await waitFor("function(){return Boolean(document.querySelector('.quick-modal'))}",'Quick Entry');assert(await c.eval("document.querySelectorAll('.quick-modal select,.quick-modal input[type=\"date\"]').length===0"),'Quick Entry has no native select/date controls');const selectInput=await c.call("function(){const input=document.querySelector('.quick-modal .owned-select-shell input');if(!input)return false;input.click();return true}");assert(selectInput,'Quick Entry owned select opens');await waitFor("function(){return Boolean(document.querySelector('.owned-select-popover [role=\"listbox\"]'))}",'owned listbox');assert(await c.eval("document.querySelector('.owned-select-popover').contains(document.activeElement)"),'listbox traps focus');assert(await c.eval("(()=>{const r=document.querySelector('.owned-select-popover').getBoundingClientRect();return r.left>=0&&r.right<=innerWidth+1&&r.top>=0&&r.bottom<=innerHeight+1})()"),'listbox stays within viewport');await c.eval("document.querySelector('.owned-select-popover [role=\"option\"]')?.click()");const dateInput=await c.call("function(){const input=document.querySelector('.quick-modal .owned-date-shell input');if(!input)return false;input.click();return true}");assert(dateInput,'Quick Entry owned date opens');await waitFor("function(){return Boolean(document.querySelector('.owned-calendar-grid[role=\"grid\"]'))}",'owned calendar');assert(await c.eval("document.querySelector('.owned-date-popover').contains(document.activeElement)"),'calendar traps focus');assert(await c.eval("document.querySelectorAll('.owned-calendar-grid [role=\"gridcell\"]').length===42"),'calendar renders six-week grid');assert(await c.eval("parseFloat(getComputedStyle(document.querySelector('.owned-date-shell input')).fontSize)>=15.9"),'owned date input uses mobile-safe font size');await c.send('Input.dispatchKeyEvent',{type:'keyDown',key:'Escape',code:'Escape',windowsVirtualKeyCode:27});await waitFor("function(){return !document.querySelector('.owned-date-popover')}",'calendar Escape close');await c.eval("document.querySelector('button[aria-label=\"Κλείσιμο καταχώρισης\"]')?.click()");
const routeChecks=[['.mobile-nav button','Αποταμίευση','Αποταμίευση','Νέα','savings-dialog'],['.mobile-more-menu button','Πιστωτική','Πιστωτική Κάρτα','Νέα αγορά','credit-dialog'],['.mobile-more-menu button','Δόσεις & Δάνεια','Δόσεις & Δάνεια','Νέο','loan-editor-dialog'],['.mobile-more-menu button','Δανεικά / Οφειλές','Δανεικά & επιστροφές','Νέα κίνηση','lending-dialog'],['.mobile-more-menu button','Πάγια','Πάγια & Συνδρομές','Νέο πάγιο','editor-dialog']];for(const [selector,label,heading,action,dialogClass] of routeChecks){if(selector.includes('mobile-more')){const more=document=>document.querySelector('button[aria-label="Περισσότερες ενότητες"]');const opened=await c.eval("document.querySelector('button[aria-label=\"Περισσότερες ενότητες\"]')?.getAttribute('aria-expanded')==='true'");if(!opened)await c.eval("document.querySelector('button[aria-label=\"Περισσότερες ενότητες\"]')?.click()");await waitFor("function(){return Boolean(document.querySelector('.mobile-more-menu'))}",'More menu')}await clickText(selector,label);await waitFor("function(heading){return (document.querySelector('#main-workspace h1')?.textContent||'').includes(heading)}",heading,[heading]);await clickText('button',action);await waitFor("function(dialogClass){return Boolean(document.querySelector('.'+dialogClass))}",`${heading} editor`,[dialogClass]);assert(await c.call("function(dialogClass){const dialog=document.querySelector('.'+dialogClass);return dialog&&dialog.querySelectorAll('select,input[type=\"date\"]').length===0&&dialog.querySelectorAll('.owned-input-shell').length>0}",[dialogClass]),`${heading} editor uses only app-owned date/select popovers`);await c.eval("document.querySelector('.editor-backdrop .icon-button')?.click()")}
c.close();console.log('Owned controls QA passed.')}finally{child.kill('SIGTERM')}
