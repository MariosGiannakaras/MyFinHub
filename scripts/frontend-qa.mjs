import { spawn, execFileSync } from 'node:child_process';
import { mkdirSync, writeFileSync } from 'node:fs';

const baseUrl=process.env.RHEOMIQ_QA_URL||'http://127.0.0.1:5173/qa.html';
const chrome=execFileSync('bash',['-lc','command -v google-chrome || command -v chromium || command -v chromium-browser'],{encoding:'utf8'}).trim();
if(!chrome)throw new Error('Chrome/Chromium is required for rendered frontend QA.');
const port=9222;
const child=spawn(chrome,['--headless=new',`--remote-debugging-port=${port}`,'--remote-debugging-address=127.0.0.1','--user-data-dir=/tmp/rheomiq-qa-chrome','--no-sandbox','--disable-gpu','about:blank'],{stdio:'ignore'});
const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
async function waitHttp(url){for(let i=0;i<60;i++){try{const response=await fetch(url);if(response.ok)return response}catch{}await sleep(250)}throw new Error(`Timed out waiting for ${url}`)}
class Cdp{
  constructor(url){this.url=url;this.id=0;this.pending=new Map()}
  async open(){await new Promise((resolve,reject)=>{this.ws=new WebSocket(this.url);this.ws.onopen=resolve;this.ws.onerror=reject;this.ws.onmessage=event=>{const message=JSON.parse(event.data);if(!message.id)return;const pending=this.pending.get(message.id);if(!pending)return;this.pending.delete(message.id);message.error?pending.reject(new Error(message.error.message)):pending.resolve(message.result)}})}
  send(method,params={}){const id=++this.id;return new Promise((resolve,reject)=>{this.pending.set(id,{resolve,reject});this.ws.send(JSON.stringify({id,method,params}))})}
  async eval(expression){const result=await this.send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.text||'Runtime evaluation failed');return result.result.value}
  async call(functionDeclaration,args=[]){const root=await this.send('Runtime.evaluate',{expression:'globalThis'});const objectId=root.result.objectId;if(!objectId)throw new Error('Unable to resolve browser global object.');const result=await this.send('Runtime.callFunctionOn',{objectId,functionDeclaration,arguments:args.map(value=>({value})),returnByValue:true,awaitPromise:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.text||'Runtime function call failed');return result.result.value}
  close(){this.ws?.close()}
}
const assert=(value,message)=>{if(!value)throw new Error(`Frontend QA assertion failed: ${message}`)};
try{
  await waitHttp(`http://127.0.0.1:${port}/json/version`);
  const target=await fetch(`http://127.0.0.1:${port}/json/new?${encodeURIComponent(baseUrl)}`,{method:'PUT'}).then(response=>response.json());
  const c=new Cdp(target.webSocketDebuggerUrl);await c.open();await c.send('Page.enable');await c.send('Runtime.enable');
  const viewport=(width,height)=>c.send('Emulation.setDeviceMetricsOverride',{width,height,deviceScaleFactor:1,mobile:width<=680});
  const navigate=async(url=baseUrl)=>{await c.send('Page.navigate',{url});for(let i=0;i<80;i++){if(await c.eval("document.readyState==='complete'&&Boolean(document.body?.innerText.trim())"))return;await sleep(100)}throw new Error(`Page not ready: ${url}`)};
  const waitFor=async(functionDeclaration,label,args=[])=>{for(let i=0;i<70;i++){if(await c.call(functionDeclaration,args))return;await sleep(100)}throw new Error(`Timed out waiting for ${label}`)};
  const waitHeading=text=>waitFor("function(text){return (document.querySelector('#main-workspace h1')?.textContent||'').includes(text)}",`heading ${text}`,[text]);
  const clickText=async(selector,text)=>{const clicked=await c.call("function(selector,text){const node=[...document.querySelectorAll(selector)].find(item=>(item.textContent||'').trim().includes(text));if(!node)return false;node.click();return true}",[selector,text]);assert(clicked,`missing clickable ${text}`)};
  const clickAria=async label=>{const clicked=await c.call("function(label){const node=[...document.querySelectorAll('button[aria-label]')].find(item=>item.getAttribute('aria-label')===label);if(!node)return false;node.click();return true}",[label]);assert(clicked,`missing aria button ${label}`)};
  const openMore=async()=>{const open=await c.eval("document.querySelector('button[aria-label=\"Περισσότερες ενότητες\"]')?.getAttribute('aria-expanded')==='true'");if(!open)await clickAria('Περισσότερες ενότητες');await waitFor("function(){return Boolean(document.querySelector('[aria-labelledby=\"mobile-more-title\"]'))}",'mobile More sheet')};
  const morePage=async(label,heading)=>{await openMore();await clickText('.mobile-more-menu button',label);await waitHeading(heading)};
  const audit=async label=>{const result=await c.eval("(()=>{const visible=e=>{const s=getComputedStyle(e),r=e.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&r.width>0&&r.height>0};const unnamed=[...document.querySelectorAll('button')].filter(visible).filter(b=>!((b.getAttribute('aria-label')||b.getAttribute('title')||b.textContent||'').trim())).map(b=>b.outerHTML.slice(0,120));const controls=[...document.querySelectorAll('input,select,textarea')].filter(visible).filter(e=>!(e.getAttribute('aria-label')||e.getAttribute('aria-labelledby')||e.closest('label')||(e.id&&document.querySelector('label[for=\"'+CSS.escape(e.id)+'\"]')))).map(e=>e.outerHTML.slice(0,120));return {unnamed,controls,overflow:document.documentElement.scrollWidth-window.innerWidth}})()");assert(!result.unnamed.length,`${label}: unnamed buttons ${result.unnamed.join(' | ')}`);assert(!result.controls.length,`${label}: unnamed controls ${result.controls.join(' | ')}`);assert(result.overflow<=1,`${label}: document overflow ${result.overflow}px`)};
  const assertMobileControlFonts=async label=>{const result=await c.eval("(()=>{const visible=e=>{const s=getComputedStyle(e),r=e.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&r.width>0&&r.height>0};return [...document.querySelectorAll('input,select,textarea')].filter(visible).map(e=>({size:parseFloat(getComputedStyle(e).fontSize),html:e.outerHTML.slice(0,90)})).filter(x=>x.size<15.9)})()");assert(!result.length,`${label}: phone controls below 16px ${JSON.stringify(result)}`)};
  const shot=async name=>{mkdirSync('/tmp/rheomiq-frontend-qa',{recursive:true});const result=await c.send('Page.captureScreenshot',{format:'png',fromSurface:true});writeFileSync(`/tmp/rheomiq-frontend-qa/${name}.png`,Buffer.from(result.data,'base64'))};

  await viewport(430,800);await navigate(`${baseUrl}?screen=login`);await waitFor("function(){return Boolean(document.querySelector('#login-title'))}",'login');await audit('login');await assertMobileControlFonts('login');
  assert(await c.eval("document.querySelector('#login-password')?.type==='password'"),'password starts masked');await clickAria('Εμφάνιση κωδικού');assert(await c.eval("document.querySelector('#login-password')?.type==='text'"),'password reveal works');

  await viewport(1440,1000);await navigate();await waitHeading('Οι λογαριασμοί μου');await audit('desktop dashboard');assert(await c.eval("Boolean(document.querySelector('.primary-balance-card .bank-brand-mark'))"),'dashboard bank branding');
  await clickText('button','Γρήγορη προσθήκη');await waitFor("function(){return Boolean(document.querySelector('[aria-labelledby=\"quick-add-title\"]'))}",'Quick Add');assert(await c.eval("document.querySelector('[aria-labelledby=\"quick-add-title\"]').contains(document.activeElement)"),'Quick Add focus');await clickAria('Κλείσιμο καταχώρισης');

  await clickText('.sidebar nav button','Συναλλαγές');await waitHeading('Συναλλαγές');await audit('desktop transactions');assert(await c.eval("getComputedStyle(document.querySelector('.desktop-finance-table')).display!=='none'&&getComputedStyle(document.querySelector('.mobile-transaction-list')).display==='none'"),'desktop transaction table');assert(await c.eval("(document.querySelector('.transaction-comment')?.textContent||'').includes('IRIS')"),'transaction comment');assert(await c.eval("!document.body.textContent.includes('Ακουστικά')&&!document.body.textContent.includes('QA Settled Purchase')"),'credit purchases stay out of generic transactions');

  await clickText('.sidebar nav button','Κάρτες');await waitHeading('Κάρτες');await audit('desktop cards');assert(await c.eval("Boolean(document.querySelector('.cards-prototype-workspace'))&&document.querySelectorAll('.cards-bank-column').length>=5&&Boolean(document.querySelector('.r-payment-card'))"),'cards workspace renders prototype bank/card surfaces');assert(await c.eval("getComputedStyle(document.querySelector('.r-payment-card')).borderRadius!=='0px'"),'interactive card stylesheet is loaded');assert(await c.eval("document.body.textContent.includes('QA Debit')&&!document.body.textContent.includes('QA Visa')&&!document.body.textContent.includes('QA Settled')"),'Cards renders stored debit card and excludes credit cards');await shot('desktop-cards');

  await clickText('.sidebar nav button','Πιστωτική');await waitHeading('Πιστωτική Κάρτα');await audit('desktop credit');assert(await c.eval("Boolean(document.querySelector('#myfinhub-card-stack .stack-card.top .payment-card'))"),'credit page renders the canonical stacked card system');assert(await c.eval("Boolean(document.querySelector('#myfinhub-card-stack .dots'))"),'credit card stack pagination renders');assert(await c.eval("Boolean(document.querySelector('.credit-usage[role=\"progressbar\"]'))"),'credit utilization');assert(await c.eval("document.body.textContent.includes('QA Visa')&&document.body.textContent.includes('Ακουστικά')&&document.body.textContent.includes('Αποπληρωμές πιστωτικής')"),'credit shares card identity and owns purchase/repayment history');await clickText('button','Νέα αγορά');await waitFor("function(){return Boolean(document.querySelector('[aria-labelledby=\"credit-purchase-title\"]'))}",'credit purchase dialog');assert(await c.eval("document.querySelector('[aria-labelledby=\"credit-purchase-title\"]').contains(document.activeElement)"),'credit dialog focus');await clickAria('Κλείσιμο αγοράς πιστωτικής');
  await clickAria('Αρχειοθέτηση κάρτας');await waitFor("function(){return Boolean(document.querySelector('.delete-slider[role=\"slider\"]'))}",'canonical archive slider');await waitFor("function(){return document.activeElement?.classList.contains('delete-slider')}",'archive slider focus');await c.eval("(()=>{const slider=document.querySelector('.delete-slider');slider.dispatchEvent(new KeyboardEvent('keydown',{key:'End',bubbles:true}));slider.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter',bubbles:true}));return true})()");await waitFor("function(){return document.body.textContent.includes('Δεν υπάρχει ενεργή πιστωτική κάρτα')&&document.body.textContent.includes('Αρχείο καρτών')}",'archived credit card leaves active deck');await clickText('button','Αρχείο καρτών');await waitFor("function(){return Boolean(document.querySelector('[aria-labelledby=\"credit-archive-title\"]'))}",'credit archive manager');assert(await c.eval("(()=>{const rows=[...document.querySelectorAll('.card-archive-row')];const visa=rows.find(row=>(row.textContent||'').includes('QA Visa'));const settled=rows.find(row=>(row.textContent||'').includes('QA Settled'));return Boolean(visa?.querySelector('button.danger')?.disabled)&&Boolean(settled)&&settled.querySelector('button.danger')?.disabled===false})()"),'archive manager blocks indebted credit and enables settled archived credit');const settledDelete=await c.call("function(){const row=[...document.querySelectorAll('.card-archive-row')].find(item=>(item.textContent||'').includes('QA Settled'));const button=row?.querySelector('button.danger');if(!button)return false;button.click();return true}");assert(settledDelete,'missing settled credit total-delete action');await waitFor("function(){return Boolean(document.querySelector('.app-confirm-dialog[role=\"alertdialog\"]'))&&document.querySelector('.app-confirm-dialog')?.textContent.includes('Ολική διαγραφή πιστωτικής')}",'total credit delete confirmation');await clickText('.app-confirm-dialog button','Ολική διαγραφή');await waitFor("function(){return Boolean(document.querySelector('.deleted-credit-history'))&&document.querySelector('.deleted-credit-history')?.textContent.includes('Διαγραμμένη κάρτα')&&document.querySelector('.deleted-credit-history')?.textContent.includes('QA Settled Purchase')&&document.querySelector('.deleted-credit-history')?.textContent.includes('QA Settled Payment')}",'deleted credit history fallback');assert(await c.eval("![...document.querySelectorAll('.card-archive-row')].some(row=>(row.textContent||'').includes('QA Settled'))"),'deleted settled credit leaves archive manager');await clickText('.card-archive-row button','Επαναφορά');await waitFor("function(){return Boolean(document.querySelector('#myfinhub-card-stack .stack-card.top .payment-card'))&&document.body.textContent.includes('Ακουστικά')}",'credit card restore with history');await clickAria('Κλείσιμο αρχείου καρτών');assert(await c.eval("document.body.textContent.includes('Ακουστικά')&&document.body.textContent.includes('Αποπληρωμές πιστωτικής')&&document.querySelector('.deleted-credit-history')?.textContent.includes('QA Settled Purchase')"),'credit archive/total-delete preserve active and deleted-card histories');

  await clickText('.sidebar nav button','Δόσεις & Δάνεια');await waitHeading('Δόσεις & Δάνεια');await audit('desktop loans');assert(await c.eval("document.querySelectorAll('.loan-list-row').length>=2"),'installment and long-term loan rows');assert(await c.eval("Boolean(document.querySelector('.installment-segments[role=\"progressbar\"]'))"),'installment progress');await clickText('button','ΒΟΗΘΕΙΑ');await waitFor("function(){return Boolean(document.querySelector('[aria-labelledby=\"loan-editor-title\"]'))}",'HELP editor');assert(await c.eval("document.querySelector('[aria-labelledby=\"loan-editor-title\"]').contains(document.activeElement)"),'loan editor focus');await clickAria('Κλείσιμο επεξεργασίας δόσεων');

  await clickText('.sidebar nav button','Πάγια');await waitHeading('Πάγια & Συνδρομές');await audit('desktop recurring');assert(await c.eval("Boolean(document.querySelector('.long-term-recurring'))&&document.body.textContent.includes('QA Long Loan')"),'long-term loans surface in Recurring');assert(await c.eval("Boolean(document.querySelector('.recurring-workspace-table [data-icon-key]'))"),'recurring semantic icons');

  await clickText('.sidebar nav button','Δανεικά / Οφειλές');await waitHeading('Δανεικά / Οφειλές');await audit('desktop lending');assert(await c.eval("Boolean(document.querySelector('.private-text'))"),'lending remains private by default');
  await clickText('.sidebar nav button','Αναφορές');await waitHeading('Αναφορές');await audit('desktop reports');assert(await c.eval("Boolean(document.querySelector('.report-operations-grid'))"),'reports operational grid');
  await clickText('.sidebar nav button','Ρυθμίσεις');await waitHeading('Ρυθμίσεις');await audit('desktop settings');assert(await c.eval("Boolean([...document.querySelectorAll('label span')].find(x=>x.textContent.includes('Πιστωτικό όριο')))"),'credit limit setting');
  await c.eval("document.querySelector('[data-qa-crash]').click()");await waitFor("function(){return Boolean(document.querySelector('.workspace-error[role=\"alert\"]'))}",'error boundary');await clickText('.workspace-error button','Dashboard');await waitHeading('Οι λογαριασμοί μου');

  await viewport(430,800);await navigate(`${baseUrl}?save=conflict`);await waitFor("function(){return Boolean(document.querySelector('.persistence-notice.conflict[role=\"alert\"]'))}",'conflict notice');await audit('conflict notice');await clickText('.persistence-notice button','Φόρτωση τελευταίας έκδοσης');await waitFor("function(){return Boolean(document.querySelector('.persistence-notice.saved'))}",'conflict recovery');

  for(const width of [320,430]){await viewport(width,812);await navigate();await waitHeading('Οι λογαριασμοί μου');await audit(`mobile dashboard ${width}`);assert(await c.eval("getComputedStyle(document.querySelector('.mobile-nav')).display==='grid'"),`mobile nav active ${width}`);await clickText('.mobile-nav button','Κάρτες');await waitHeading('Κάρτες');await audit(`mobile cards ${width}`);await assertMobileControlFonts(`mobile cards ${width}`);assert(await c.eval("Boolean(document.querySelector('.cards-prototype-workspace .r-payment-card'))&&!document.body.textContent.includes('QA Visa')&&!document.body.textContent.includes('QA Settled')"),`stored cards visible and credit cards excluded on phone ${width}`);await morePage('Πάγια','Πάγια & Συνδρομές');await audit(`mobile recurring ${width}`)}

  await viewport(375,812);await navigate();await waitHeading('Οι λογαριασμοί μου');
  assert(await c.eval("[...document.querySelectorAll('.mobile-nav button')].every(node=>node.getBoundingClientRect().height>=44)"),'mobile navigation touch targets');
  await clickText('.mobile-nav button','Συναλλαγές');await waitHeading('Συναλλαγές');await audit('mobile transactions');await assertMobileControlFonts('mobile transactions');
  await clickText('.mobile-nav button','Αποταμίευση');await waitHeading('Αποταμίευση');await audit('mobile savings');
  await clickText('.mobile-nav button','Κάρτες');await waitHeading('Κάρτες');await audit('mobile cards');await shot('mobile-cards');
  await morePage('Πιστωτική','Πιστωτική Κάρτα');await audit('mobile credit');assert(await c.eval("Boolean(document.querySelector('#myfinhub-card-stack .stack-card.top .payment-card'))"),'canonical credit card stack stays visible on mobile');assert(await c.eval("document.querySelector('.semantic-table-wrap').scrollWidth>=document.querySelector('.semantic-table-wrap').clientWidth"),'credit history remains contained');
  await morePage('Δόσεις & Δάνεια','Δόσεις & Δάνεια');await audit('mobile loans');await assertMobileControlFonts('mobile loans');
  await morePage('Δανεικά / Οφειλές','Δανεικά / Οφειλές');await audit('mobile lending');
  await morePage('Πάγια','Πάγια & Συνδρομές');await audit('mobile recurring');assert(await c.eval("Boolean(document.querySelector('.mobile-pay-action'))"),'recurring primary payment action');
  await clickText('.long-term-recurring button','Άνοιγμα Δόσεων & Δανείων');await waitHeading('Δόσεις & Δάνεια');
  await morePage('Αναφορές','Αναφορές');await audit('mobile reports');
  await morePage('Ρυθμίσεις','Ρυθμίσεις');await audit('mobile settings');await assertMobileControlFonts('mobile settings');

  await viewport(667,375);await navigate();await waitHeading('Οι λογαριασμοί μου');await audit('landscape dashboard');await openMore();assert(await c.eval("document.querySelector('.mobile-more-menu').getBoundingClientRect().height<window.innerHeight"),'landscape More constrained');await clickAria('Κλείσιμο μενού');
  await viewport(430,800);await navigate(`${baseUrl}?motion=reduced`);await waitHeading('Οι λογαριασμοί μου');assert(await c.eval("document.documentElement.dataset.motion==='reduced'"),'reduced motion state');await audit('reduced motion');

  c.close();console.log('Rendered frontend QA passed.');
}finally{child.kill('SIGTERM')}