import { execFileSync, spawn } from 'node:child_process';
import { mkdirSync, writeFileSync } from 'node:fs';

const baseUrl=process.env.RHEOMIQ_QA_URL||'http://127.0.0.1:5173/qa.html';
const evidenceDir=process.env.MYFINHUB_UX_EVIDENCE_DIR||'/tmp/myfinhub-account-metadata-qa';mkdirSync(evidenceDir,{recursive:true});
const configured=process.env.MYFINHUB_QA_USE_FALLBACK==='1'?process.env.MYFINHUB_QA_FALLBACK_BROWSER:process.env.MYFINHUB_QA_PRIMARY_BROWSER;
const chrome=configured||execFileSync('bash',['-lc','command -v google-chrome || command -v chromium || command -v chromium-browser'],{encoding:'utf8'}).trim();if(!chrome)throw new Error('Chrome/Chromium is required for account metadata QA.');
const port=9250;const profile='/tmp/myfinhub-account-metadata-qa-chrome';const child=spawn(chrome,['--headless=new',`--remote-debugging-port=${port}`,'--remote-debugging-address=127.0.0.1',`--user-data-dir=${profile}`,'--no-sandbox','--disable-gpu','--disable-dev-shm-usage','about:blank'],{stdio:'ignore'});
const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));async function waitHttp(url){for(let i=0;i<100;i++){try{const response=await fetch(url);if(response.ok)return}catch{}await sleep(150)}throw new Error(`Timed out waiting for ${url}`)}
class Cdp{constructor(url){this.url=url;this.id=0;this.pending=new Map();this.listeners=new Map()}async open(){await new Promise((resolve,reject)=>{this.ws=new WebSocket(this.url);this.ws.onopen=resolve;this.ws.onerror=reject;this.ws.onmessage=event=>{const message=JSON.parse(event.data);if(message.id){const pending=this.pending.get(message.id);if(!pending)return;this.pending.delete(message.id);message.error?pending.reject(new Error(message.error.message)):pending.resolve(message.result);return}for(const fn of this.listeners.get(message.method)||[])fn(message.params)}})}send(method,params={}){const id=++this.id;return new Promise((resolve,reject)=>{this.pending.set(id,{resolve,reject});this.ws.send(JSON.stringify({id,method,params}))})}on(method,fn){const list=this.listeners.get(method)||[];list.push(fn);this.listeners.set(method,list)}async call(functionDeclaration,args=[]){const root=await this.send('Runtime.evaluate',{expression:'globalThis'});const result=await this.send('Runtime.callFunctionOn',{objectId:root.result.objectId,functionDeclaration,arguments:args.map(value=>({value})),returnByValue:true,awaitPromise:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.text||'Runtime function call failed');return result.result.value}close(){this.ws?.close()}}
const assert=(value,message)=>{if(!value)throw new Error(`Account metadata QA assertion failed: ${message}`)};
try{
  await waitHttp(`http://127.0.0.1:${port}/json/version`);const target=await fetch(`http://127.0.0.1:${port}/json/new?${encodeURIComponent(baseUrl)}`,{method:'PUT'}).then(response=>response.json());const c=new Cdp(target.webSocketDebuggerUrl);await c.open();await c.send('Page.enable');await c.send('Runtime.enable');await c.send('Network.enable');const runtimeErrors=[];const failedRequests=[];c.on('Runtime.exceptionThrown',params=>runtimeErrors.push(params.exceptionDetails?.text||'runtime exception'));c.on('Network.loadingFailed',params=>{if(!params.canceled&&params.errorText!=='net::ERR_ABORTED')failedRequests.push(`${params.errorText||'network failure'} [${params.type||'unknown'}]`)});
  const viewport=(width,height)=>c.send('Emulation.setDeviceMetricsOverride',{width,height,deviceScaleFactor:1,mobile:width<=680});const waitFor=async(fn,label,args=[])=>{for(let i=0;i<120;i++){if(await c.call(fn,args))return;await sleep(100)}throw new Error(`Timed out waiting for ${label}`)};const screenshot=async name=>{const shot=await c.send('Page.captureScreenshot',{format:'png',captureBeyondViewport:false});writeFileSync(`${evidenceDir}/${name}.png`,Buffer.from(shot.data,'base64'))};const navigate=async page=>{const url=new URL(baseUrl);url.searchParams.set('page',page);await c.send('Page.navigate',{url:url.href});await waitFor("function(){return document.readyState==='complete'||document.readyState==='interactive'}",`${page} document`)};const scrollTo=async selector=>{const ok=await c.call("function(selector){const node=document.querySelector(selector);node?.scrollIntoView({block:'start',inline:'nearest'});return Boolean(node)}",[selector]);assert(ok,`missing scroll target ${selector}`);await sleep(160)};const noOverflow=async label=>{const overflow=await c.call("function(){return Math.max(document.documentElement.scrollWidth,document.body.scrollWidth)-innerWidth}");assert(overflow<=1,`${label} horizontal overflow ${overflow}px`)};const touchTargets=async(label,selector)=>{const offenders=await c.call("function(selector){return [...document.querySelectorAll(selector)].filter(el=>{const r=el.getBoundingClientRect();if(!r.width||!r.height||getComputedStyle(el).visibility==='hidden'||el.disabled)return false;return r.width<40||r.height<40}).map(el=>({label:el.getAttribute('aria-label')||(el.textContent||'').trim().slice(0,40),w:Math.round(el.getBoundingClientRect().width),h:Math.round(el.getBoundingClientRect().height)}))}",[selector]);assert(offenders.length===0,`${label} touch targets below 40px: ${JSON.stringify(offenders)}`)};const setInput=async(selector,value)=>{const ok=await c.call("function(selector,value){const input=document.querySelector(selector);if(!input)return false;const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set;setter.call(input,value);input.dispatchEvent(new Event('input',{bubbles:true}));input.dispatchEvent(new Event('change',{bubbles:true}));return true}",[selector,value]);assert(ok,`missing input ${selector}`)};const click=async selector=>{const ok=await c.call("function(selector){const node=document.querySelector(selector);node?.click();return Boolean(node)}",[selector]);assert(ok,`missing control ${selector}`)};const trustedClick=async selector=>{const point=await c.call("function(selector){const node=document.querySelector(selector);if(!node)return null;node.scrollIntoView({block:'nearest',inline:'nearest'});const r=node.getBoundingClientRect();return {x:r.left+r.width/2,y:r.top+r.height/2}}",[selector]);assert(point&&Number.isFinite(point.x)&&Number.isFinite(point.y),`missing trusted-click control ${selector}`);await c.send('Input.dispatchMouseEvent',{type:'mouseMoved',x:point.x,y:point.y});await c.send('Input.dispatchMouseEvent',{type:'mousePressed',x:point.x,y:point.y,button:'left',clickCount:1});await c.send('Input.dispatchMouseEvent',{type:'mouseReleased',x:point.x,y:point.y,button:'left',clickCount:1})};

  console.log('Account metadata QA: Dashboard configured, missing and copyable IBAN');await viewport(1440,1000);await navigate('dashboard');await waitFor("function(){return Boolean(document.querySelector('[data-account-iban=\"piraeus-payroll\"]'))}",'dashboard IBAN');const dashboardState=await c.call("function(){const payroll=document.querySelector('[data-account-iban=\"piraeus-payroll\"]')?.textContent||'';const cash=document.querySelector('[data-account-iban=\"cash\"]')?.textContent||'';return {payroll,cash,copies:document.querySelectorAll('.account-iban-copy').length}}");assert(dashboardState.payroll.includes('GR16 •••• •••• 0695'),'configured payroll IBAN is masked on Dashboard');assert(dashboardState.cash.includes('Δεν έχει οριστεί'),'missing IBAN is explicit');assert(dashboardState.copies>=2,'configured IBANs expose copy controls');await trustedClick('.dashboard-account-card[data-account-id="piraeus-payroll"] .account-iban-copy');await waitFor("function(){return Boolean(document.querySelector('.dashboard-account-card[data-account-id=\"piraeus-payroll\"] [role=status]'))}",'copy confirmation');await noOverflow('account metadata dashboard desktop');await screenshot('account-metadata-dashboard-desktop');

  console.log('Account metadata QA: Settings editor saves independently from FinanceData');await navigate('settings');await waitFor("function(){return Boolean(document.querySelector('.account-metadata-settings'))}",'account metadata settings');await scrollTo('.account-metadata-settings');const payrollInput='.account-metadata-row[data-account-metadata-row="piraeus-payroll"] input';await waitFor("function(selector){const input=document.querySelector(selector);return Boolean(input&&input.value.includes('GR16'))}",'payroll metadata value',[payrollInput]);await setInput(payrollInput,'GB82 WEST 1234 5698 7654 32');await click('.account-metadata-row[data-account-metadata-row="piraeus-payroll"] .save-button');await waitFor("function(){const row=document.querySelector('.account-metadata-row[data-account-metadata-row=\"piraeus-payroll\"]');return (row?.textContent||'').includes('Το IBAN αποθηκεύτηκε.')}",'metadata save confirmation');const savedValue=await c.call("function(selector){return document.querySelector(selector)?.value||''}",[payrollInput]);assert(savedValue==='GB82 WEST 1234 5698 7654 32','saved IBAN stays normalized and human-readable');await noOverflow('account metadata settings desktop');await screenshot('account-metadata-settings-desktop');

  console.log('Account metadata QA: mobile Dashboard keeps full IBAN and copy target');await viewport(375,812);await navigate('dashboard');await waitFor("function(){return Boolean(document.querySelector('[data-account-iban=\"piraeus-payroll\"]'))}",'mobile dashboard IBAN');await scrollTo('.dashboard-primary-grid');await noOverflow('account metadata dashboard mobile');await touchTargets('account metadata dashboard mobile','.dashboard-primary-grid .account-iban-copy');const mobilePayroll=await c.call("function(){return document.querySelector('[data-account-iban=\"piraeus-payroll\"]')?.textContent||''}");assert(mobilePayroll.includes('GR16 •••• •••• 0695'),'mobile payroll IBAN stays masked on Dashboard');await screenshot('account-metadata-dashboard-mobile');

  console.log('Account metadata QA: mobile Settings editor stays usable');await navigate('settings');await waitFor("function(){return Boolean(document.querySelector('.account-metadata-settings'))}",'mobile account metadata settings');await scrollTo('.account-metadata-settings');await noOverflow('account metadata settings mobile');await touchTargets('account metadata settings mobile','.account-metadata-settings button,.account-metadata-settings input');await screenshot('account-metadata-settings-mobile');await viewport(320,700);await scrollTo('.account-metadata-settings');await noOverflow('account metadata settings narrow mobile');await touchTargets('account metadata settings narrow mobile','.account-metadata-settings button,.account-metadata-settings input');

  assert(runtimeErrors.length===0,`runtime exceptions: ${runtimeErrors.join(' | ')}`);assert(failedRequests.length===0,`network loading failures: ${failedRequests.join(' | ')}`);c.close();console.log('Account metadata rendered QA passed.');
}finally{child.kill('SIGTERM')}
