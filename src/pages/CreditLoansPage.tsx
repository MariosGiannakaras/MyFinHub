import { CreditCard, Landmark, Pencil, Plus, ReceiptText, X } from 'lucide-react';
import { useState } from 'react';
import { accountBalances } from '../lib/domain';
import { money } from '../lib/format';
import { loanDraftError } from '../lib/inputSemantics';
import { loanPaidCount } from '../lib/loans';
import type { FinanceData, Loan } from '../types';

export function CreditLoansPage({data,asOf,onCardPurchase,onCardPayment,onUpsertLoan,onPayLoan}:{data:FinanceData;asOf:string;onCardPurchase:()=>void;onCardPayment:()=>void;onUpsertLoan:(loan:Loan)=>void;onPayLoan:(loan:Loan)=>void}){
 const balances=accountBalances(data,asOf); const debt=Math.abs(Math.min(0,balances['credit-card']||0)); const loans=[...(data.seed.loans??[]).map(l=>data.state.loanOverrides?.[l.id]??l),...(data.state.customLoans??[])];
 const [edit,setEdit]=useState<Loan|null>(null); const [editError,setEditError]=useState('');
 const startNew=()=>{setEditError('');setEdit({id:`loan-${Date.now()}`,name:'',total:0,installment:0,installments:0,day:'',provider:'',schedule:[],paidCount:0,source:'user',accountingMode:'expense-per-installment'})};
 const startEdit=(loan:Loan)=>{setEditError('');setEdit({...loan})};
 const save=()=>{if(!edit)return;const error=loanDraftError(edit);if(error){setEditError(error);return}onUpsertLoan(edit);setEdit(null);setEditError('')};
 return <div className="page-stack"><section className="page-heading"><div><span className="eyebrow">LIABILITIES</span><h1>Κάρτες & Δόσεις</h1><p>Η αγορά είναι expense· η εξόφληση πιστωτικής είναι transfer προς liability. Οι δόσεις δηλώνουν ρητά το accounting mode.</p></div><div className="heading-actions"><button className="secondary" onClick={onCardPayment}><CreditCard size={16}/> Εξόφληση κάρτας</button><button className="save-button" onClick={onCardPurchase}><CreditCard size={17}/> Αγορά με κάρτα</button></div></section><section className="credit-hero neo-raised"><div><span>Virtual liability</span><b>{money.format(debt)}</b><small>Τρέχουσα οφειλή από RheomIQ card events</small></div><CreditCard size={54}/></section><section className="section-title"><div><span>Δόσεις & δάνεια</span><b>Η διαγραφή payment event μειώνει αυτόματα το derived paid count</b></div><button className="text-button" onClick={startNew}><Plus size={14}/> Νέο</button></section><section className="loan-cards">{loans.map(l=>{const paid=loanPaidCount(data,l);const progress=l.installments?paid/l.installments:0;return <article className="panel neo-raised" key={l.id}><div className="loan-title"><span className="account-mark"><Landmark/></span><div><h3>{l.name}</h3><small>{l.provider||'Δόσεις'} · {(l.accountingMode||'expense-per-installment')==='liability-repayment'?'Εξόφληση πιστωτικής':'Expense ανά δόση'}</small></div><b>{money.format(l.installment)}</b></div><div className="loan-progress"><i style={{width:`${progress*100}%`}}/></div><div className="loan-stats"><span><b>{paid}/{l.installments}</b> πληρωμένες</span><span><b>{money.format(Math.max(0,l.total-paid*l.installment))}</b> υπόλοιπο</span></div><div className="loan-actions"><button onClick={()=>startEdit(l)}><Pencil/> Επεξεργασία</button><button className="pay" disabled={paid>=l.installments} onClick={()=>onPayLoan(l)}><ReceiptText/> Πληρωμή δόσης</button></div></article>})}</section>{edit?<section className="panel neo-raised inline-editor"><div className="panel-head"><div><span>Ρυθμίσεις δόσεων</span><small>Το accounting mode αποτρέπει διπλομέτρηση αγοράς και εξόφλησης.</small></div><button className="icon-button" onClick={()=>{setEdit(null);setEditError('')}}><X/></button></div><div className="settings-form editor-grid"><label><span>Όνομα</span><input value={edit.name} onChange={e=>setEdit({...edit,name:e.target.value})}/></label><label><span>Συνολικό ποσό</span><input type="number" min="0.01" step="0.01" value={edit.total||''} onChange={e=>setEdit({...edit,total:Number(e.target.value)})}/></label><label><span>Ποσό δόσης</span><input type="number" min="0.01" step="0.01" value={edit.installment||''} onChange={e=>setEdit({...edit,installment:Number(e.target.value)})}/></label><label><span>Αριθμός δόσεων</span><input type="number" min="1" step="1" value={edit.installments||''} onChange={e=>setEdit({...edit,installments:Number(e.target.value)})}/></label><label><span>Ήδη πληρωμένες (baseline)</span><input type="number" min="0" max={edit.installments||0} step="1" value={edit.paidCount??0} onChange={e=>setEdit({...edit,paidCount:Number(e.target.value)})}/></label><label><span>Ημέρα</span><input value={edit.day||''} onChange={e=>setEdit({...edit,day:e.target.value})}/></label><label><span>Πάροχος</span><input value={edit.provider||''} onChange={e=>setEdit({...edit,provider:e.target.value})}/></label><label className="wide"><span>Accounting mode</span><select value={edit.accountingMode||'expense-per-installment'} onChange={e=>setEdit({...edit,accountingMode:e.target.value as Loan['accountingMode']})}><option value="expense-per-installment">Κάθε δόση είναι πραγματικό expense</option><option value="liability-repayment">Η αγορά έχει ήδη καταγραφεί στην πιστωτική — οι δόσεις εξοφλούν τη virtual κάρτα</option></select></label></div>{editError?<div className="form-error">{editError}</div>:null}<div className="editor-actions"><button className="secondary" onClick={()=>{setEdit(null);setEditError('')}}>Ακύρωση</button><button className="save-button" onClick={save}>Αποθήκευση</button></div></section>:null}</div>;
}
