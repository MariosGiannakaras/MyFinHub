import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { selectCategoryTotals, selectMonthlyFlow } from '../lib/selectors';
import { money } from '../lib/format';
import type { FinanceData } from '../types';
export function ReportsPage({data,month}:{data:FinanceData;month:string}){const cats=selectCategoryTotals(data,month).slice(0,12);const f=selectMonthlyFlow(data,month);return <div className="page-stack"><section className="page-heading"><div><span className="eyebrow">REPORTING</span><h1>Αναφορές</h1><p>Τα splits τροφοδοτούν τις πραγματικές κατηγορίες και τα refunds αφαιρούν spending.</p></div></section><section className="report-kpis"><div className="neo-raised"><span>Έσοδα</span><b className="positive">{money.format(f.income)}</b></div><div className="neo-raised"><span>Έξοδα</span><b className="negative">{money.format(f.expense)}</b></div><div className="neo-raised"><span>Αποταμίευση</span><b>{money.format(f.saving)}</b></div><div className="neo-raised"><span>Refunds</span><b>{money.format(f.refunds)}</b></div></section><section className="panel neo-raised"><div className="panel-head"><div><span>Κατηγορίες</span><small>Net spending μετά από refunds</small></div></div>{cats.length?<><div aria-hidden="true"><ResponsiveContainer width="100%" height={390}><BarChart data={cats} layout="vertical" margin={{left:20,right:35}}><CartesianGrid stroke="#dbe4f0" strokeDasharray="3 3" horizontal={false}/><XAxis type="number" tick={{fontSize:10,fill:'#52627d'}}/><YAxis type="category" dataKey="name" width={130} tick={{fontSize:10,fill:'#52627d'}}/><Tooltip formatter={(v)=>money.format(Number(v))}/><Bar dataKey="value" fill="#2f6fed" radius={[0,7,7,0]} isAnimationActive={false}/></BarChart></ResponsiveContainer></div><details className="chart-alt" open><summary>Ποσά ανά κατηγορία</summary><ul className="chart-alt-list">{cats.map(c=><li key={c.name}><span>{c.name}</span><b>{money.format(c.value)}</b></li>)}</ul></details></>:<div className="empty-state">Δεν υπάρχουν έξοδα ανά κατηγορία για την επιλεγμένη περίοδο.</div>}</section></div>}
