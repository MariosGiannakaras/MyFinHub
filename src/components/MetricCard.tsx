import { motion } from 'framer-motion';
import type { ReactNode } from 'react';
export function MetricCard({label,value,hint,icon,tone='blue',progress,reduceMotion=false}:{label:string;value:string;hint?:string;icon?:ReactNode;tone?:'blue'|'cyan'|'green'|'red'|'violet';progress?:number;reduceMotion?:boolean}){return <motion.article className={`metric-card tone-${tone}`} whileHover={reduceMotion?undefined:{y:-3}} transition={{duration:reduceMotion?0:.16}}><div className="metric-head"><span>{label}</span><span className="metric-icon" aria-hidden="true">{icon}</span></div><strong>{value}</strong>{hint?<small>{hint}</small>:null}{typeof progress==='number'&&Number.isFinite(progress)?<div className="mini-progress" role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={Math.round(Math.max(0,Math.min(1,progress))*100)} aria-label={`${Math.round(progress*100)}%`}><i style={{width:`${Math.min(100,Math.max(0,progress*100))}%`}}/></div>:null}</motion.article>}
